# Diabetes-predictor
this is a diabetes detector which uses naive bayes model for training. It was trained on the dataset which is uploaded on this repo. I have trained 3 models here, naive bayes model, MLP and a custom implementation of MLP which are available in the ipynb file.
